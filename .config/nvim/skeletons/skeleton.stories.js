// @flow
// # Imports

import * as React from "react";
import { storiesOf } from "@storybook/react";
import `!p snip.rv = snip.fn.split('.')[0]` from "./`!p snip.rv = snip.fn.split('.')[0]`.js";

storiesOf("`!p snip.rv = snip.fn.split('.')[0]`", module).add("`!p snip.rv = snip.fn.split('.')[0]`", () => <`!p snip.rv = snip.fn.split('.')[0]` $3>$2</`!p snip.rv = snip.fn.split('.')[0]`>);
