// @flow
// # Imports

import * as React from "react";
import { View } from "react-native";
import system from "@rebass/components";

// # Types

type localProps = {
    $3
};


// # Styled

const `!p snip.rv = snip.fn.split('.')[0]` = ({ $2 }: localProps) => {
  // # Funcs

    return (
        <View>
           ${1:${VISUAL}}
        </View>

    )
};



export default `!p snip.rv = snip.fn.split('.')[0]`
