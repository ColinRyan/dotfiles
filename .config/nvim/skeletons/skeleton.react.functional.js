// # Imports

import * as React from "react"



// # Styled


const `!p snip.rv = snip.fn.split('.')[0]` = ({ $1 }) => {
  // # Funcs

  
  $0
  return ($2)
}

export default `!p snip.rv = snip.fn.split('.')[0]`
